{% for row in rows %}
    {%
    set row_classes = [
      default_row_class ? 'views-row',
      'col-md-3',
      'col-sm-6',
      'team-members',
      'wow',
      'fadeInUp'
    ]
  %}
    <div{{row.attributes.addClass(row_classes)}}>
        {{- row.content -}}
    </div>
{% endfor %}
