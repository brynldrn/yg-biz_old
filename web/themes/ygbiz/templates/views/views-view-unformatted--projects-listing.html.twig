{% for row in rows %}
    {%
    set row_classes = [
      default_row_class ? 'views-row',
      'col-md-4',
      'col-sm-12',
      'col-xs-12',
      'categories',
    ]
  %}
    <div{{row.attributes.addClass(row_classes)}}>
        {{- row.content -}}
    </div>
{% endfor %}
